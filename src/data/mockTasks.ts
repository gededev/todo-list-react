import Task from "../models/Task";

const mockTasks: Task[] = [
    {task: "Do the laundry", done: false},
    {task: "Do the coding exercise", done: true},
    {task: "Eat a meal instead of an energy drink", done: false}
]

export default mockTasks